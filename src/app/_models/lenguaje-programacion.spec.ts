import { LenguajeProgramacion } from './lenguaje-programacion';

describe('LenguajeProgramacion', () => {
  it('should create an instance', () => {
    expect(new LenguajeProgramacion()).toBeTruthy();
  });
});
